const Product = (pCategory, pTitle, pImage, pName, pPrice) => {
  this.pCategory = pCategory;
  this.pTitle = pTitle;
  this.pImage = pImage;
  this.pName = pName;
  this.pPrice = pPrice;
};

module.exports = Product;
